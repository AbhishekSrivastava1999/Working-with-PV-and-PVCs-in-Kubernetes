apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name:
  namespace:
  labels:
    app: mysql
spec:
  resources:
    requests:
	storage: 1Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: 
