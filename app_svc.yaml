apiVersion: v1
kind: Service
metadata:
  name: nport
  namespace: lab3
  labels:
    app: employees
spec:
  type: LoadBalancer
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30001
  selector:
    app: employees